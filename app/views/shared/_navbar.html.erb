<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag 'logo.png' %>
  <% end %>

    <ul class="navbar-nav d-none d-sm-flex">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to "Alle Erinnerungen", baby_path(Baby.find(1)), class: "nav-link mr-4" %>
        </li>
        <li class="nav-item dropdown">
        <% if current_user.photo.attached? %>

          <%= cl_image_tag current_user.photo.key, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Profil bearbeiten", edit_user_registration_path, class: "dropdown-item" %>
            <%= link_to "Ausloggen", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
      <% end %>

        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>

    <ul class="navbar-nav d-sm-none">
      <% if user_signed_in? %>

      <%= link_to "Alle Erinnerungen", baby_path(Baby.find(1)), class: "btn btn-primary" %>



        <!-- <li class="nav-item dropdown">
          <%#= cl_image_tag current_user.photo.key, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%#= link_to "Alle Erinnerungen", baby_path(Baby.find(1)), class: "dropdown-item" %>
            <%#= link_to "Profil bearbeiten", edit_user_registration_path, class: "dropdown-item" %>
            <%#= link_to "Ausloggen", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
 -->

      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "btn btn-primary" %>
        </li>
      <% end %>
    </ul>
  </div>
